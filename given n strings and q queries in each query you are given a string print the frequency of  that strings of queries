#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() 
{
    int n,i;
    cin >> n;
    unordered_map <string, int> m;
    for(i=0;i<n;i++)
    {
        string s;
        cin>>s;
        m[s]++;
    }
    int q;
    cin>>q;
    for(i=0;i<q;i++)
    {
        string s1;
        cin>>s1;
       // auto it: m;
        cout<< m[s1]<<endl;
    }
}
////using unordered maps :
